<UserControl x:Class="IRCBot.LoginPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <GroupBox Header="Login" Margin="12 0" BorderThickness="1" VerticalAlignment="Center">
            <StackPanel>
                <Grid Margin="8 0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center" Text="Server:" />
                    <TextBox Margin="8 0" x:Name="textboxServer" Grid.Column="1" Text="Test" />
                    <TextBlock Grid.Column="2" VerticalAlignment="Center" Text="Port:" />
                    <TextBox Margin="8 0 0 0" x:Name="textboxPort" Grid.Column="3" Width="70"/>
                </Grid>
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>
