<Window x:Class="IRCBot.DebugWindow"
        Style="{StaticResource FlatWindow}"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        Title="Debugger"
        Width="525"
        Height="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer x:Name="scroller" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="itemsList" Margin="8">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Foreground="{StaticResource brushText}" Text="{Binding}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <TextBox x:Name="textboxMessage" Grid.Row="1" Margin="8 8 173 8" KeyUp="textboxMessage_KeyUp" />
        <Button Margin="8 8 140 8" Grid.Row="1" Click="buttonSend_Click" HorizontalAlignment="Right" Padding="2 2">Send</Button>
        <Button Margin="8" Grid.Row="1" Click="ButtonClose_Click" HorizontalAlignment="Right" Padding="40 2">Close</Button>
    </Grid>
</Window>
